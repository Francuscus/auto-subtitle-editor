# Critical: Install numpy FIRST with exact version to avoid binary incompatibility
numpy==1.26.4

# Core dependencies (order matters)
gradio==4.44.0
torch==2.8.0
torchaudio==2.8.0

# Audio transcription - use transformers Whisper (no ctranslate2!)
transformers>=4.35.0
accelerate>=0.20.0
librosa>=0.10.0

# Spanish NLP for verb detection (install after core packages)
spacy>=3.7.0
https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.7.0/es_core_news_sm-3.7.0-py3-none-any.whl

# Video export
ffmpeg-python>=0.2.0

# Optional (acentos integrated)
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
